DB_HOST=localhost  # Default is localhost, but update if using a remote database
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=your_database_name
PORT=your_preferred_port  # Default is 5050, but you can change it
