# Backend Environment Variables Example

# Cloud Database Connection (Replace with actual value in your .env)
DATABASE_URL=your-railway-database-url

# Server Port (default is 5050, can be changed)
PORT=5050

# Secret Key for JWT Authentication (generate your own for security)
JWT_SECRET=your-secret-key